# Deployment Checklist — MY(suru) BUS

## ✅ Already Deployed (Current State — March 2026)

### Backend (Render)
- [x] Docker multi-stage build working
- [x] `render.yaml` Blueprint configured
- [x] PostgreSQL database provisioned (Render free tier)
- [x] Environment variables set (DATABASE_URL, JWT_SECRET auto-generated, ALLOWED_ORIGINS)
- [x] Migrations run automatically on every deploy
- [x] Health check endpoint at `/health`
- [x] Graceful shutdown (SIGTERM/SIGINT)
- [x] Pino structured logging
- [x] Sentry integration (needs SENTRY_DSN env var to activate)
- [x] Rate limiting (in-memory, sufficient for current scale)
- [x] CORS locked to `https://my-suru-bus.vercel.app`
- [x] Admin user created on prod DB

### Admin Dashboard (Vercel)
- [x] Connected to GitHub repo (auto-deploys on push to `dev`)
- [x] `NEXT_PUBLIC_API_URL` set in Vercel dashboard
- [x] Login working with prod credentials

### Database
- [x] Schema created via idempotent migrations
- [x] Sample data seeded (15 stops, 5 routes, 10 drivers, 10 buses, 15 schedules, 13 trips)
- [x] All constraints and indexes in place

---

## ⏳ Still Todo

### Mobile Apps
- [ ] Build driver app APK: `cd driver-app && eas build --profile preview --platform android`
- [ ] Build passenger app APK: `cd passenger-app && eas build --profile preview --platform android`
- [ ] Install and test on physical Android device
- [ ] End-to-end test: admin creates trip → driver app → passenger tracks

### Hardening (Before Real Users)
- [ ] Set up UptimeRobot to ping `/health` every 5 min (prevents Render cold start)
- [ ] Extend driver JWT from 24h → 7 days (or add refresh token)
- [ ] Set real `SENTRY_DSN` in Render env vars for error tracking
- [ ] Set up Render DB backup strategy (before 90-day free tier expiry)

### Future (Before Scaling)
- [ ] Custom domain (`api.mysurubus.com`)
- [ ] HTTPS via custom domain (already handled by Render, need domain)
- [ ] Move to Render paid plan or AWS when funded
- [ ] Switch rate limiter to Redis-backed for multi-instance safety
- [ ] Add JWT refresh tokens
- [ ] Add automated tests (at minimum: auth + trip flow)

---

## Security Status

| Item | Status |
|---|---|
| JWT_SECRET | ✅ Auto-generated by Render (strong) |
| bcrypt password hashing | ✅ rounds=12 |
| SQL injection | ✅ Parameterized queries throughout |
| CORS | ✅ Locked to Vercel domain |
| Rate limiting | ✅ In-memory (sufficient for POC) |
| HTTPS | ✅ Provided by Render/Vercel |
| .env in git | ✅ Gitignored |
| Admin default password | ⚠️ Change `Admin@123` before public demo |

---

## Prod Credentials

| Service | URL / Login |
|---|---|
| Backend | https://mysurubus-backend.onrender.com |
| Admin Dashboard | https://my-suru-bus.vercel.app |
| Admin Login | admin@mybus.com / Admin@123 |
| DB (pgAdmin) | dpg-d6iu2mua2pns73e6ds50-a.singapore-postgres.render.com |
| All seed drivers | phone 987654321X / Driver@123 |

---

**Last Updated:** March 2026
